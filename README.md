# skillable-for-Trainocate
Skillable使用時の注意点(トレノケートオリジナルコース)

# 見出し
\#で指定した見出しと、リポジトリ名が異なる場合、リポジトリへのリンクがH1タグで表示されます。

これを避けるため、リポジトリ名と同じ名前の \# 見出しを追加して、リンクを切ります。

見出しとしては表示されるので、これを削除するため、見出しを \<span aria-hidden="true" class="sr-only"\> で囲みます。
